Programming is fun I love programming

df1 = spark.read.text("file_name")

df2 = df1.withcolumn("new_col", explode(split(col("value")," ")).alias("word")).drop(col("value"))

df3 = df2.groupBy("new_col").count()

df4 = df3.filter(col("count") > 1)
df4.show()



emp_df =emp_id,emp_name, dept_id,salary
emp_.count()df
 
DEPT TBL
dpt_df = dept_id, dept_name
dpt_df.count()
 
compute:
agg_df = Headcount: No of emp in a dept
flag: if headcount < 10 then 'small"
headcount > 10 then "large"
agg_df.count()

1) matching records in both the files -
set File 1 end date = 8888/1/1
and file 2 record keep as is
2) non matching in File1 which are not there in file 2 ==> end date them in File 1
3) Non matching in File2 which are not in File 1 ==> keep as it is (don't do anything)






